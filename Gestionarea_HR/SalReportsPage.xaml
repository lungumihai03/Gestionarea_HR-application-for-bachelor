<Page
    x:Class="Gestionarea_HR.SalReportsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Gestionarea_HR"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
   >

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Raport Salarial" FontSize="20" Margin="0,0,0,10"/>
        <StackPanel Grid.Row="1" Orientation="Vertical" Spacing="10">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="Nume:" VerticalAlignment="Center"/>
                <TextBox x:Name="textBox_nume" Width="200" TextChanged="textBox_nume_TextChanged"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="Prenume:" VerticalAlignment="Center"/>
                <TextBox x:Name="textBox_prenume" Width="200" TextChanged="textBox_prenume_TextChanged"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="Lună start:" VerticalAlignment="Center"/>
                <ComboBox x:Name="comboBox_monthstart" Width="200" 
                          ItemsSource="{x:Bind ViewModel.Months, Mode=OneWay}" 
                          SelectionChanged="comboBox_monthstart_SelectionChanged"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="Lună end:" VerticalAlignment="Center"/>
                <ComboBox x:Name="comboBox_monthend" Width="200" 
                          ItemsSource="{x:Bind ViewModel.Months, Mode=OneWay}" 
                          SelectionChanged="comboBox_monthend_SelectionChanged"/>
            </StackPanel>
        </StackPanel>
        <Button x:Name="button_generate" Grid.Row="2" Content="Generează Raport" Click="button_generate_Click" Margin="0,10,0,0" IsEnabled="False"/>

        <controls:DataGrid x:Name="dataGrid1" Grid.Row="3" Margin="0,10,0,0" AutoGenerateColumns="False" ItemsSource="{x:Bind ViewModel.ReportItems, Mode=OneWay}" IsReadOnly="True">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="Auto"/>
                <controls:DataGridTextColumn Header="EmployeeID" Binding="{Binding EmployeeID}" Width="Auto"/>
                <controls:DataGridTextColumn Header="Nume" Binding="{Binding Nume}" Width="Auto"/>
                <controls:DataGridTextColumn Header="Prenume" Binding="{Binding Prenume}" Width="Auto"/>
                <controls:DataGridTextColumn Header="Salary" Binding="{Binding Salary}" Width="Auto"/>
                <controls:DataGridTextColumn Header="SP" Binding="{Binding SP}" Width="Auto"/>
                <controls:DataGridTextColumn Header="SC" Binding="{Binding SC}" Width="Auto"/>
                <controls:DataGridTextColumn Header="ST" Binding="{Binding ST}" Width="Auto"/>
                <controls:DataGridTextColumn Header="MED" Binding="{Binding MED}" Width="Auto"/>
                <controls:DataGridTextColumn Header="IV" Binding="{Binding IV}" Width="Auto"/>
                <controls:DataGridTextColumn Header="SB" Binding="{Binding SB}" Width="Auto"/>
                <controls:DataGridTextColumn Header="Month" Binding="{Binding Month}" Width="Auto"/>
                <controls:DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="Auto"/>
                <controls:DataGridTextColumn Header="Ore_t" Binding="{Binding Ore_t}" Width="Auto"/>
                <controls:DataGridTextColumn Header="Ore_l" Binding="{Binding Ore_l}" Width="Auto"/>
                <controls:DataGridTextColumn Header="SN" Binding="{Binding SN}" Width="Auto"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <Button x:Name="button_export" Grid.Row="4" Content="Exportă Raport" Click="button_export_Click" Margin="0,10,0,0" IsEnabled="False"/>
    </Grid>
</Page>